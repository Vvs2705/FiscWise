# FiscWise Backend Dependencies
# Python 3.12+

# Core Framework
fastapi==0.139.0
uvicorn[standard]==0.30.6

# Database & ORM
sqlalchemy[asyncio]==2.0.51
asyncpg>=0.30.0
alembic==1.13.3

# Data Validation
pydantic>=2.13.4
pydantic-settings==2.5.2
email-validator==2.1.1

# Authentication & Security
python-jose[cryptography]==3.3.0
# passlib 1.7.x is incompatible with bcrypt >= 4.0 (reads bcrypt.__about__
# which was removed).  We call bcrypt directly in app/core/security.py.
bcrypt>=4.0.0
python-multipart==0.0.12
google-auth==2.29.0
requests==2.34.2
# Two-Factor Authentication (TOTP / Google Authenticator)
pyotp==2.9.0
qrcode[pil]==7.4.2

# Async & Utilities
httpx==0.27.2
python-dotenv==1.0.1

# Redis (Cache & Rate Limiting)
redis==5.1.1
hiredis==3.0.0

# Logging & Observability
loguru==0.7.2
sentry-sdk[fastapi]>=2.0.0

# Document Parsing / Generation
pdfplumber==0.11.10
fpdf2==2.8.3
openpyxl==3.1.5
python-docx==0.8.11

# Task Scheduling
apscheduler==3.10.4
pytz==2024.1

# Testing (Development)
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
aiosqlite==0.19.0
