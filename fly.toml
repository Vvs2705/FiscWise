# Fly.io configuration for FiscWise
# Deploy with: flyctl deploy

app = "contaflow"
primary_region = "gru"  # São Paulo - Brasil (mais próximo)
console_command = "/bin/bash"

[build]
dockerfile = "./backend/Dockerfile"

[env]
ENVIRONMENT = "production"
DEBUG = "False"
APP_NAME = "FiscWise"
APP_VERSION = "1.0.0"
PUBLIC_URL = "https://contaflow.fly.dev"
ALLOWED_ORIGINS = "https://fiscwise-frontend.vercel.app,https://fiscwise.com.br"
SUPABASE_URL = "https://lkgmgbieottygodrdubi.supabase.co"

[http_service]
internal_port = 8000
force_https = true
auto_stop_machines = false
auto_start_machines = true
min_machines_running = 1

[checks]
[checks.http]
grace_period = "120s"
interval = "15s"
method = "GET"
path = "/api/v1/health"
protocol = "http"
timeout = "10s"
type = "http"

# Secrets — set via: flyctl secrets set KEY=value
# JWT_SECRET_KEY       (required)
# DATABASE_URL         (required)
# SUPABASE_SERVICE_KEY (required)
# ANTHROPIC_API_KEY    (optional — only if AI features are active)

[[vm]]
cpu_kind = "shared"
cpus = 1
memory_mb = 512
